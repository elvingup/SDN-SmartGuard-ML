global:
  scrape_interval: 5s # Frequência de coleta (deve bater com o loop do monitor_ia)

scrape_configs:
  - job_name: 'sdn_smartguard_ia'
    static_configs:
      - targets: ['localhost:8000'] # Endereço do nosso monitor_ia.py
